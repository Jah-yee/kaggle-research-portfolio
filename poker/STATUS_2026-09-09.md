# Poker status — 2026-09-09

## Verified access and data

- Kaggle account entry is confirmed and API download/submission access works.
- The official 276 MB archive was downloaded and passed archive integrity and
  eight-table schema checks.
- The extracted data contains 2,000,000 hands, 18,609,028 actions, 1,860
  confirmed development labels, 1,817 evidence labels, and 112,540 evaluation
  pairs.
- Development supervision contains 372 confirmed targets and 1,488 confirmed
  non-targets. No unlisted development pair was converted into a negative.

## E001 remote anchor

- File: `submissions/poker_residual_v1.csv`.
- SHA-256:
  `319f304dd4bc3716a13e4ebf9f543b19fc1409b0a46a9cac004c3000a04407b8`.
- Submission ID: `56106882`.
- Status: complete.
- Public score: **0.44388**.
- Pre-submission checks: exact 112,540-pair coverage; 112,540 unique continuous
  risk values; no null scores; all 562,700 evidence cells are distinct legal
  shared evaluation hands for their submitted pair.

The public score failed the preregistered 0.75 gate. No later candidate was
submitted. The main diagnosis is confirmed-negative selection shift: the
confirmed-only whole-pool OOF Pair AP of 0.948 does not transfer to ranking the
much broader evaluation population. The weaker rule evidence MAP@5 proxy of
0.135 is a secondary bottleneck.

## Evidence validation status

E002's reported Evidence MAP@5 of 0.398819 is now classified as an oracle-family
component ceiling, not an end-to-end candidate score: its OOF path chooses each
ranker using the true behavior family, whereas production uses a predicted
family; its family-specific folds also do not match the common risk/behavior
outer folds. No E002-specific OOF receipt survives in the current work
directory. E006's 0.443468 remains a reproducible public-notebook research
control with cross-fitted family routing, but it bundles a different public
risk/PU/evidence stack and is not an owned promoted candidate. The owned
end-to-end Evidence MAP@5 is therefore currently unmeasured; the E001 rule
proxy of 0.135258 remains only the remote anchor's diagnostic.

A live Kaggle notebook inventory at 2026-09-09 16:46 CST returned only the
three already archived sources: official metric, official tutorial, and the
public PU ranker. No additional public method family is currently available;
the reusable research pattern is common outer-fold, cross-fitted
predicted-family evidence routing, not any published score by itself.

EV000 now establishes that an owned strict validation is structurally
possible without touching unknown or confirmed-non-target pairs. All 372
confirmed targets have three to five legal organizer evidence hands (1,817
total), and the frozen B001 whole-pool folds support three nested inner folds
inside every outer-training partition. Both time directions remain non-empty.
The limiting cell is early-to-late coordinated isolation: only 47 pairs have
late evidence, split 7/11/12/10/7 across the five outer folds. The two
seven-pair cells are fragile and cannot support silent exclusions or narrow
uncertainty claims. EV000 is receipt-only; it fit no model and measured no
evidence performance.

The preregistration input audit found one additional, now-frozen edge case.
All 20 proposed fields are finite on the 45,129 confirmed-target candidate
rows, but one early directed-transfer query in outer fold 3 has one candidate
and that hand is evidence. Early coverage is therefore 347 evidence-bearing
queries but 346 pairwise-trainable queries; the query must stay visible with
zero preference rows rather than being silently dropped or counted as
trainable. Full and late coverage remain 372/372 and 264/264 trainable.

The original documentation-only preregistration is preserved as
`EVP-DRAFT-01` at SHA-256 `7a170fd0...0301`. A later tie-policy amendment made
the then-current code/report/draft hashes differ from the earlier independent
PASS, so that audit is retained as `PROVISIONAL_HASH_MISMATCH` rather than
silently overwritten. No model or performance metric had been run.

The replacement is separately versioned as `EVP-DRAFT-02`, SHA-256
`a689703b...0054`, and binds current EV000 implementation/report hashes
`7518ed87...a49d` / `27a8245e...4726`. A complete independent reconstruction
now passes at audit-v2 SHA-256 `85adea3b...09fc`: 11 source hashes, all
fold/time/readiness counts, and H0 tie structure match. Exact random-tie AP@5
was checked against 55 exhaustive synthetic cases; deterministic output ties
may use gameplay `started_at` only, with no identifier or row-order fallback.
The amendment retains the nested B001 inner routing that prevents true-family
selection leakage. It remains unauthorized for implementation or execution.

A separate pool-provenance companion audit passed without modifying the v2
binding. All 45,129 target candidate rows and 1,817 evidence rows match their
B001 receipt pool; every query belongs to exactly one pool/fold. The 372 target
queries occupy 245 pools, including 62 pools containing multiple behavior
families and 53 within-family multi-query pools. Any future uncertainty audit
must therefore resample `table_id` clusters, never independent hands or
queries. Companion receipt SHA-256 is `eb11f452...b4545`; it does not authorize
the preregistration run.

A bootstrap-support companion then found one unresolved v2 specification.
Under literal with-replacement pool sampling, early-to-late fold-0 isolation
has a 0.00145855 probability of an empty family draw—about 7.29 empty cells in
5,000 replicates—and fold 4 contributes about 2.32. v2 does not say whether to
reject, omit, or zero-fill these undefined family/macro draws. Status is
therefore `REVIEW_REQUIRED_BOOTSTRAP_AMBIGUITY`; implementation remains
forbidden. Receipt SHA-256 is `71b02ce0...092a`. A paired Bayesian pool-weight
bootstrap is documented only as a possible supervisory amendment, not as an
adopted rule.

Supervisory review has now preserved v2 as historical but withdrawn it from
methodology execution. `EVP-DRAFT-03` replaces the ambiguous rule with 5,000
paired Bayesian `table_id`-cluster weight draws, shared across H0/L0/L1 and
every family, with an exact RNG schedule, weighted aggregation, linear 5%/95%
quantiles, and no reject/omit/zero-fill path. A proposal-only structural audit
verified positive denominators in all 45 view/fold/family cells; weight-stream
SHA-256 is `c833ac7d...1975c`.

A final supervision-authorized wording review now names the output only a
“90% paired Bayesian cluster-weight sensitivity interval.” It explicitly is
not a frequentist confidence interval and has no validated coverage rate;
positive lower sensitivity bounds are perturbation-robustness gates only. The
draft separately distinguishes query-weighted overall, within-family
pool-weighted means, unweighted macro-family means, and query-level paired
deltas. Current v3 SHA-256 is `54776c54...23e5`; its updated static audit is
`PASS_DRAFT_ONLY_AWAITING_SUPERVISION` with execution state
`DRAFT_ONLY_AWAITING_SUPERVISION`, SHA-256 `a45c55aa...7b91`. The prior v3 and
audit hashes remain historical entries in the withdrawal chain. No
implementation or execution is authorized.

Supervision later authorized implementation only, still without result-bearing
execution. The source is now present and hash-bound: core mechanics SHA-256
`5871093c...005e`, one-run executor SHA-256 `5a0f5aec...5fad`, core tests
`68171171...f57`, and executor tests `b907b921...8d9a`. Compilation passed,
all 17 EVP tests passed, and the full repository suite passed 23/23. The tests
include 55 exhaustive random-tie cases and a wholly synthetic nested
route/selection/outer-score integration; synthetic model fits occurred, but no
real evidence model was fitted and no aggregate evidence performance was
computed or viewed.

The source-only structural audit reverified all 12 source hashes, the exact
372/45,129/1,817 target/candidate/evidence population, all fold/family/time
coverage, and the retained early zero-preference query. It reproduced the
frozen weight stream `c833ac7d...1975c` and the no-header negative control
`08ee7564...e1a`. The historical hash's known limitation remains explicit: it
does not itself bind pool IDs or shapes, so source and exact pool/count checks
remain mandatory companion controls. The prior static audit remains
byte-identical at `a45c55aa...7b91`; its historical
`implementation_created=false` field is superseded for current state by the
new containment receipt, not edited in place.

Containment status is `PASS_IMPLEMENTATION_PRESENT_UNEXECUTED`, receipt
SHA-256
`f04b10963b9e466cda2cb819a668344464c7efd7ead98ebb772b6879b1e5f75c`.
The executor rejects the current
`IMPLEMENTATION_ONLY_APPROVED` decision and requires a later exact
`RESULT_BEARING_EXECUTION_APPROVED` receipt before it can consume its one run.
No result receipt, consumption marker, E-number, candidate promotion,
evaluation access, or submission exists.

Independent source review has since rejected that v3 implementation without
executing it. Current status is `SOURCE_REJECTED_UNEXECUTED`; decision receipt
SHA-256 is
`90bb142996798652cc64adb863c93ff845452fb56363500cab969b68de213a1d`.
The rejected core and runner remain byte-identical. The four blocking findings
are canonical containment binding, unambiguous C-selection family semantics,
strict primary→attribution→time compute/data-access ordering, and metric-local
coverage with missing/low-coverage stops.

A versioned design-only repair now exists as `EVP-DRAFT-04`, SHA-256
`3a9f20936f4173f72cb4ff867d296eb2945b43ca40cac0ee587b7fb4b83da22c`.
It chooses inner-OOF query-weighted overall random-tie MAP@5 for C selection,
with no family grouping or family field in the selection frame. It hard-freezes
sequential stages: primary failure forbids L0 attribution and every time-source
read; attribution failure forbids all time access. Canonical receipts and
source-audit hashes—not caller-selected paths—must be bound by any future
authorization.

The separate metric-local coverage contract has SHA-256
`4e821092161be0b0c80892af41ed61718e0a4930562b748a826da8a8bf9001e6`.
A read-only self-check matched all 72 frozen cells across three views. Every
method metric and contrast must carry adjacent query/candidate/evidence/pool
counts; exact mismatch, missing cells, or time fold-family coverage below
7/458/17/6 stops before the metric. v4 is awaiting independent method audit;
it has not authorized source changes or execution.

## Offline experiment outcome

- E003 bagged PU, E004/E005 normalization, E008 outcome impact, E009 influence,
  E010 global LambdaRank, and E011 hard-negative weighting all failed their
  frozen promotion gates and were not submitted.
- E007 interaction features were the only candidate to improve full-period AP
  and both time directions, especially the isolation family, but failed the
  preregistered absolute time-transfer threshold. It remains diagnostic only.
- E012's numerical Stage A comparison improved confirmed AP by 0.009996 and
  its P95/P99 stress rates, but the method audit invalidated promotion. The
  calibration constant and emitted scores came from different selector fits,
  unknown case-control weighting changed the base rate, SCAR was unverified,
  and 13.49% of labelled OOF scores clipped to one. E012 is recorded only as a
  biased-PU sensitivity signal; its time gate was not run.
- B001 established an exact confirmed-only behavior baseline without changing
  risk or evidence. Positive-family accuracy is 0.943548, but coordinated
  isolation recall is the weakest at 0.891304 and falls to 0.804348/0.782609
  across the two time directions. Random-tie Behavior MAP is 0.854915 when all
  confirmed rows are assigned a family, versus 0.262344 under the frozen 4%
  sparse policy. These are diagnostic comparisons only; no activation rate was
  promoted after viewing the grid. Confirmed behavior coverage is limited to
  397 of 400 pools.

## Current safe state

E001 remains the sole remote anchor and submission 56106882 remains the only
Kaggle submission. Unknown development pairs were never assigned ground-truth
negative labels. The v3 implementation is source-rejected and unexecuted. The
immediate safe next decision is an independent method audit of v4; before a
PASS, even implementation changes are forbidden. Real execution, evaluation
scoring, E-number assignment, candidate promotion, and submission remain
forbidden. The earlier behavior-conditioned risk proposal is likewise not
authorized by this status note.
