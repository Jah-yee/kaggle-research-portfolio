# Portfolio manual-advance receipt — 2026-09-17 09:18 CST

## Scope

This was a bounded manual advancement round for Poker, Hyperspectral Object
Detection (HOD), ARC-AGI-2, and Biohub.  It was permitted to advance at most
two lines and only when the action could produce a new scientific receipt or a
gate-qualified submission.  No waiting loop, speculative training, or new
submission was authorized.

## Fresh official observations

Authenticated Kaggle CLI observations at 2026-09-17 09:13–09:18 CST:

| Competition | Deadline UTC | Joined | Teams | Current account state |
|---|---:|---:|---:|---|
| Poker | 2026-09-20 22:00 | yes | 292 | submission `56106882` COMPLETE, Public `0.44388`, leaderboard row rank 246 |
| HOD | 2026-09-24 16:00 | yes | 216 | no submissions; leader `0.67943` |
| ARC-AGI-2 | 2026-11-02 23:59 | yes | 2051 | submission `56287444` remains PENDING; leader `76.94` |
| Biohub | 2026-09-29 23:59 | yes | 3625 | submission `56108120` COMPLETE, Public `0.944`, leaderboard row rank 943; leader `0.970` |

The exact downloaded public-leaderboard snapshots were generated by Kaggle at
`2026-09-17T01:13:36`.

## Candidate decisions

### Poker — terminal, no execution

- Stage: sealed after incomplete v4.2 result-bearing run.
- Scientific hypothesis: family-conditioned evidence ranking might add stable
  MAP@5 information after strict pool/time isolation.
- Hacking leverage: none remains legally/scientifically available under the
  consumed one-run authorization.
- Terminal evidence:
  `poker/work/EVP_v4_2_incomplete_unknown_termination.json` records only outer
  folds 0/1, no aggregate metric, a consumed authorization, and
  `rerun_allowed=false`.
- Acceptance gate: the preregistered full run, MAP@5 and time-direction gates
  cannot be evaluated from partial routes.
- Decision: do not resume, extrapolate, submit, or create v4.3.  Reopening
  requires explicit user approval of a wholly new mechanism and budget.

### HOD — terminal attachment failure, no execution

- Stage: joined, zero submissions, model hypothesis untested.
- Scientific hypothesis: one COCO-pretrained pseudo-RGB `[5,8,13]` detector
  could establish a useful single-checkpoint baseline under spatially grouped
  validation.
- Hacking leverage: a clean, official competition-source mount was the only
  acceptable way to reach one fast T4 baseline.
- Fresh authenticated UI observation: the competition still has no Code tab;
  direct `/code` returns `We can't find that page`; the existing private
  notebook's Add Input -> Competition Datasets search returns zero results for
  the exact competition title and URL.  No source was attached, no notebook
  version was saved or run, and no download/training/submission occurred.
- Acceptance gate: a remote attachment plus mount receipt must pass before any
  model work.  It did not.
- Decision: preserve the prior v1/v2 fail-closed receipts and seal HOD until an
  official auditable attachment or transport route appears.  A 15.65 GB local
  download is not a substitute for that gate.

### ARC-AGI-2 — one anchor already in flight, no duplicate action

- Stage: exact public Qwen anchor dry-run passed; competition submission
  `56287444` is still PENDING.
- Scientific hypothesis: a byte-identical public single-model anchor can
  reproduce at least Public `29.0` within the 12-hour offline limit before any
  owned method is evaluated.
- Hacking leverage: exact dependency/runtime reproduction, not tuning.
- Existing run evidence: private Kaggle dry-run version 1 completed in 1,697 s,
  produced a valid 120-task/172-output `submission.json`, and passed all frozen
  schema/runtime/hash gates.
- Acceptance gate: Public score `>=29.0`; below the floor archives the anchor.
- Decision: do not push, repair, or submit another kernel while `56287444` is
  pending.  Read its terminal state once when Kaggle publishes it; do not poll
  in a waiting loop.

### Biohub — frozen, no execution

- Stage: current Public `0.944`; strict one-epoch embryo-isolated OOF score
  `0.0300153`.
- Scientific hypothesis: lowering detector threshold from `0.965` to the
  preregistered `0.950` might reduce severe node underprediction in both embryo
  directions.
- Hacking leverage: reuse the two frozen BH-0003 checkpoints without training.
- Current constraint: BH-0004 is only `PREREGISTERED_NO_RUN`; its weights are
  not locally present, a new inference transport/kernel has not passed
  preflight, and the underlying OOF is far below a credible route to the
  leaderboard gap.
- Acceptance gate: both directional scores and combined score must strictly
  exceed `0.0558759`, `0.0186467`, and `0.0300153`, with zero receipt errors;
  even that is scientific calibration evidence, not automatic submission
  authority.
- Decision: do not spend this deadline round rebuilding transport for a
  low-value threshold probe.  Reopen only if an independent, licensed lineage
  shows at least `+0.010` over Public `0.944`, as required by the portfolio
  control plane.

## Round outcome

No candidate met the pre-execution gate.  Therefore zero new training jobs,
kernel versions, submissions, or waiting processes were started.  This is the
correct terminal result of the round, not inactivity: the fresh observations
closed HOD's UI route, prevented an illegal Poker rerun, prevented a duplicate
ARC submission, and rejected a low-information Biohub transport rebuild.

